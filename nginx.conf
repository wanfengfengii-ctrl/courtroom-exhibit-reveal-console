server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index console.html;

    location = / {
        return 302 /console.html;
    }

    location / {
        try_files $uri $uri/ /console.html;
    }
}
